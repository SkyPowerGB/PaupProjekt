@using PaupProjekt.Models
@model List<uslugeTab>
@{
    ViewBag.Title = "DodajUslugu";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>DodajUslugu</h2>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Naziv</th>
            <th>Cijena</th>

        </tr>

    </thead>
    <tbody>
        @foreach (uslugeTab usluga in Model)
        {
        <tr>
            <td>@usluga.nazivUsluga</td>
           
            <td>@usluga.cijenaUsluga</td>
            <td>@Html.ActionLink("+","IzavanjeRacuna","Racun",new { idNarudzbe=(int)ViewBag.id , idUsluge = usluga.UslugaID }, new {@class="btn btn-success" })</td>
        </tr>

        }
    </tbody>

</table>